# Dependency Review

Adicionado aos dois projetos em `pull_request` para main usando `actions/dependency-review-action@v5.0.0` por SHA, com `fail-on-severity: moderate`.

STATUS: PENDENTE_DE_EXECUÇÃO_NO_GITHUB. Se o plano/configuração não disponibilizar Dependency Review, a execução deverá ser registrada como `BLOCKED_BY_PLATFORM`; não há downgrade configurado.
