# Dependency Review

Adicionado aos dois projetos em `pull_request` para main usando `actions/dependency-review-action@v5.0.0` por SHA, com `fail-on-severity: moderate`.

RESULTADO: Skill Kit PASS. MCP `BLOCKED_BY_PLATFORM`: a action oficial retornou que Dependency Review não é suportado enquanto o Dependency Graph estiver desabilitado, com link para `settings/security_analysis`. Não há downgrade configurado.
